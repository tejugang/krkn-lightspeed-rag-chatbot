name: Issue Management

on:
  pull_request_target:
    types: [opened, reopened]
  issues:
    types: [opened, labeled]

jobs: 
    issue-management: 
        uses: tejugang/krkn-lightspeed-rag-chatbot/.github/workflows/issue-management.yml@main
