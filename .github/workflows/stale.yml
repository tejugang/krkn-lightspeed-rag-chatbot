name: Stale
on:
  # Runs daily at midnight UTC
  schedule:
    - cron: '0 0 * * *'
  # Allows manual triggering from the Actions tab for testing
  workflow_dispatch:
jobs:
  close-issue:
    permissions:
      issues: write
      pull-requests: write
    uses: tejugang/test-actions/.github/workflows/stale.yml@main
    secrets: inherit
